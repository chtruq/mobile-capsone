import apiClient from "./apiClient";

export const getListConsignmentsByAccount = async () => {
  try {
    // const response = await apiClient.get("/consignments/get-all");
    // return response.data;
  } catch (error) {
    console.error(error);
  }
};
